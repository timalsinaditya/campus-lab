ORG 00H

MOV 20H, #03FH
MOV 21H, #006H
MOV 22H, #05BH
MOV 23H, #04FH
MOV 24H, #066H
MOV 25H, #06DH
MOV 26H, #07DH
MOV 27H, #007H
MOV 28H, #07FH
MOV 29H, #06FH

DISPLAY:
	MOV R1, #020H
	MOV R0, #01H
	MOV R2, #18H
AGAIN:
	MOV R6, #05H
OUTER:
	MOV R7, #010H
INNER:
	MOV P0, #07CH
	MOV P2, #01H
	LCALL SDELAY
	MOV P2, #00H
	
	MOV P0, #079H
	MOV P2, #02H
	LCALL SDELAY
	MOV P2, #00H
	
	MOV A, R0
	MOV B, #0AH
	
	DIV AB

	ADD A, #20H
	MOV R1, A
	MOV P0, @R1
	MOV P2, #04H
	LCALL SDELAY
	MOV P2, #00H

	MOV A, B
	ADD A, #20H
	MOV R1, A
	MOV P0, @R1
	MOV P2, #08H
	LCALL SDELAY
	MOV P2, #00H

	DJNZ R7, INNER
	DJNZ R6, OUTER

	INC R0
	DJNZ R2, AGAIN

SJMP DISPLAY

SDELAY:
	MOV R5, #018H
	SOUTER:
	MOV R4, #018H

	SINNER:
	DJNZ R4, SINNER
	DJNZ R5, SOUTER
RET
	
END

